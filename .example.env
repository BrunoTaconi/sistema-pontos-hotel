# Database connection string (use your Neon credentials)
DATABASE_URL="postgresql://<usuario>:<senha>@<host>/<nome_do_banco>?sslmode=require"

# Resend API key (used for sending emails)
RESEND_API_KEY="re_<sua_chave_api_resend>"

# Email credentials (used for authentication and notifications)
EMAIL_USER="seuemail@seudominio.com"
EMAIL_PASSWORD="sua_senha_de_email"

# Secret key for JWT authentication
JWT_SECRET="sua_chave_secreta_segura"
